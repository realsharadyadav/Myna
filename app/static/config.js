// Backend API origin. Empty string = same-origin (local dev, or when the
// backend still serves these pages itself). The static frontend deploy
// (render.yaml) overwrites this file at build time with the real backend URL.
window.MYNA_API_BASE = "";
